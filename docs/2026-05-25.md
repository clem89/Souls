# 업무 보고 — 2026-05-25

## 오늘 진행한 작업
- `PlayerAnimator.cs` 임시 색상 피드백 구현 — Idle/이동/닷지/무적(i-frame)/공격/패리/리포스트/사망 8가지 상태를 SpriteRenderer 색상으로 표현
- `AttackHitbox.cs` 생성 — CircleCollider2D 트리거 기반 충돌 판정, HashSet으로 중복 타격 방지
- `SkillEffectData.cs` ScriptableObject 생성 — damage, attackRange, lifetime, poolSize 보유
- `PlayerLoadout.cs` ScriptableObject 생성 — 장착 스킬 목록 정의
- `SkillEffectPool.cs` 생성 — 프리팹 기반 오브젝트 풀 (plain C# class)
- `SkillEffectPoolManager.cs` 생성 — 씬 로드 시 PlayerLoadout 읽어 풀 초기화
- `PlayerCombat.cs` 업데이트 — OverlapCircleAll 제거, SpawnHitbox()로 교체
- `AttackHitboxPool.cs` 제거 — SkillEffectPoolManager로 완전 대체
- Assets 불필요 파일 정리 — TutorialInfo/, Readme.asset, Mobile_RPAsset, Mobile_Renderer, SampleSceneProfile 삭제

## 결정된 사항
- **공격 판정 방식**: OverlapCircleAll 즉발 → AttackHitbox 트리거 충돌 방식으로 전환 (타격감 및 비주얼 이펙트 연동 목적)
- **히트박스 크기**: 프리팹 Inspector에서 설정 — 런타임 override 없음
- **PlayerLoadout 구조**: ScriptableObject로 장착 스킬 정의, 나중에 세이브/DB 시스템 연동 시 Register() 호출부만 교체
- **SkillEffectPool**: MonoBehaviour 아닌 plain C# class — SkillEffectPoolManager가 자식 GameObject를 만들어 관리

## 미결 사항
- TestEffect 프리팹에 AttackHitbox + CircleCollider2D 구성 Unity 에디터에서 확인 필요
- LightSlash.asset → effectPrefab 연결, DefaultLoadout.asset → lightAttack 연결 체인 점검 필요
- 이펙트 프리팹 실제 비주얼 미완성 (SpriteRenderer/Animator 미추가)

## 다음 작업 예정
- **이펙트 프리팹 셋업 및 전투 테스트**
  1. TestEffect 프리팹 — AttackHitbox + CircleCollider2D 확인
  2. LightSlash.asset에 프리팹 연결, DefaultLoadout.asset에 스킬 연결 확인
  3. Play 모드에서 공격 → 히트박스 스폰 → 데미지 확인
  4. 이펙트 프리팹에 SpriteRenderer 추가하여 실제 비주얼 구성
- **PlayerLoadout 슬롯 확장** (heavyAttack, parry 등)
