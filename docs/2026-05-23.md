# 업무 보고 — 2026-05-23

## 오늘 진행한 작업
- Animator Controller 스테이트 머신 완성 (Idle/Run/Attack1-3/Dodge/Parry/Riposte/Dead)
  - Run→Idle, Attack1→Attack2→Attack3 콤보 체인, Riposte→Idle 트랜지션 추가
  - HasExitTime 이슈 검토 및 수정 (3회 재검토)
- PlayerAnimator 스크립트 작성
  - GetComponentInChildren<Animator> 방식으로 메시 자식 Animator 참조
  - SerializeField 필드 제거 → GetComponent 자동 참조로 단순화
- PlayerCombat: `IsAttacking`, `ComboStep` 프로퍼티 공개
- PlayerHealth: `OnDeath` 이벤트 추가, `_isDead` 중복 사망 방지
- 3D 캐릭터(Ch24_nonPBR) 연결 시도 → 메테리얼 미연결, Apply Root Motion 이슈 확인
- **3D → 탑뷰 2D 전환 결정 및 실행**
  - PlayerCamera: 마우스 회전 제거, 탑뷰 고정 Orthographic follow 카메라로 교체
  - PlayerController: 카메라 의존 이동 제거, 월드 기준 이동으로 단순화
  - PlayerAnimator: Animator 제거, SpriteRenderer flipX 방향 전환으로 교체
  - LockOnSystem: `camForward` → `transform.forward` (플레이어 전방 기준 타겟 탐색)
  - `Assets/Characters/` 3D FBX·텍스처·메테리얼·Animator Controller 전체 삭제

## 결정된 사항
- **게임 뷰 방식**: 3D → 탑뷰 2D (아이작 스타일) 전환 확정
- **캐릭터 비주얼**: 원형 스프라이트(Unity 기본 Knob) 플레이스홀더 사용, 추후 스프라이트로 교체
- **이동 방식**: 카메라 상대 이동 → 월드 기준 이동 (WASD = X/Z축 직접 대응)
- **Animator 구조**: 추후 스프라이트 애니메이션 추가 시 PlayerAnimator 확장 예정

## 미결 사항
- Unity 씬 작업 미완료 (코드만 교체된 상태)
  - Camera Orthographic 전환, Rotation (90, 0, 0) 설정
  - 플레이어 오브젝트에 SpriteRenderer(원) 자식 추가
  - LockOnSystem Inspector 재연결

## 다음 작업 예정
- **Unity 씬 탑뷰 세팅 완료 후 플레이 테스트**
  1. Main Camera → Projection: Orthographic, Rotation: (90, 0, 0), Size: 8~10
  2. 플레이어 자식에 SpriteRenderer + Knob 스프라이트 연결
  3. PlayerCamera Target, LockOnSystem PlayerCamera 필드 재연결
  4. 실행 후 이동·전투·카메라 동작 확인
- 탑뷰 기준 전투 범위(OverlapSphere) 시각 확인 및 조정
- 탑뷰용 간단한 방(Room) 레이아웃 구성
