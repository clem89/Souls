# 업무 보고 — 2026-05-28

## 오늘 진행한 작업

- **KnightAnimatorGenerator.cs 작성** (`Assets/Editor/KnightAnimatorGenerator.cs`)
  - Knight 스프라이트시트에서 AnimationClip 8개 + AnimatorController 자동 생성 Editor 스크립트
  - Animator 파라미터: Speed(float), AttackStep(int), IsParrying(bool), RiposteReady(bool), HurtTrigger(trigger), IsDead(bool)
  - 9개 상태: Idle, Walk, Attack1-3, Parry, Riposte, Hurt, Dead
- **PlayerAnimator.cs 교체** (`Assets/Scripts/Player/PlayerAnimator.cs`)
  - 색상 기반 프로토타입 → Animator 파라미터 드라이버로 전면 교체
  - IFrame 중 alpha 깜빡임 코드 유지, Hurt 이벤트 PlayerHealth.OnHpChanged 구독으로 처리
- **방향 전환 버그 수정**
  - `PlayerController`: 이동 방향 기반 `_rb.rotation` 코드 제거 (사이드뷰 스프라이트에 부적합)
  - `PlayerAnimator`: `MoveDirection.x` 기반 `flipX` 처리 추가
- **3타 애니메이션 조기 종료 버그 수정**
  - `KnightAnimatorGenerator`: `Trans` 헬퍼에 `exitTime=1f` 추가, Attack→Idle 전환에 적용
  - 공격 애니메이션이 끝까지 재생된 후 Idle 복귀 (다음 콤보 입력 시 AnyState가 즉시 가로챔)
- **카메라 orthographicSize 조정** (Unity에서 직접)
  - 캐릭터 Scale ×10 방식 → Camera orthographicSize = 1로 대체

## 결정된 사항

- **스프라이트 방향 처리**: `_rb.rotation` 회전 방식 폐기, `SpriteRenderer.flipX`만 사용
- **공격 애니메이션 타이밍**: Attack→Idle 전환에 `hasExitTime=true, exitTime=1f` 적용. PlayerCombat 공격 지속시간(0.5s)과 애니메이션 길이(Attack01=0.7s, Attack02=1.0s, Attack03=1.1s) 불일치는 시각적으로 해결, 게임플레이 타이밍은 추후 검토
- **카메라 방식**: Unity 2D 표준인 orthographicSize 기반으로 확정

## 미결 사항

- `[Unity 수동]` Tools > Animator Generator > Generate Knight Animator **재실행** 필요 (exitTime 수정 반영)
- `[Unity 수동]` Play Mode 최종 테스트 (3타 끝까지 재생, flipX 방향 확인)
- `Assets/Animations/` 생성 파일 커밋 (Generator 재실행 후)
- Player 계층 구조 리팩터링 구현 미착수

## 다음 작업 예정

- **Player 계층 구조 리팩터링**
  1. 방향 결정: Visual(SpriteRenderer+Animator) 자식 분리 — 코드 변경 없이 가능
  2. 선택적: Collider2D 자식 분리 — IDamageable 피격 파이프라인용 브릿지 스크립트 필요
  3. Unity에서 Animator Generator 재실행 후 Assets/Animations/ 커밋
- 참고: 현재 PlayerAnimator는 이미 `GetComponentInChildren`을 사용하므로 Visual 자식 분리는 즉시 적용 가능
