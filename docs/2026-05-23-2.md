# 업무 보고 — 2026-05-23 (2차)

## 오늘 진행한 작업
- **전체 스크립트 Unity 2D 전환** (3D → Rigidbody2D / Physics2D)
  - `PlayerController`: `CharacterController` 제거 → `Rigidbody2D`, XZ 이동 → XY 이동, Z축 회전으로 방향 처리 (`transform.up` = facing)
  - `PlayerDodge`: `CharacterController` 제거 → `Rigidbody2D`, `linearVelocity` + `WaitForFixedUpdate` 방식
  - `PlayerCombat`: `Physics.OverlapSphere` → `Physics2D.OverlapCircleAll`, 공격 방향 `transform.forward` → `transform.up`
  - `PlayerAnimator`: flipX 제거 (transform 회전이 대신 처리)
  - `DummyEnemy`: `Renderer/Material.color` → `SpriteRenderer.color` (그로기 시각 피드백)
  - `DummyEnemyAttack`: `Vector3.Distance` → `Vector2.Distance`
  - `LockOnSystem`: `Physics.OverlapSphere` → `Physics2D.OverlapCircleAll`, `transform.forward` → `transform.up`
  - `PlayerCamera`: XZ 팔로우 → XY 팔로우, Z 고정
- **충돌 튕김 버그 수정**: `linearVelocity` → `MovePosition` (벽 슬라이딩 개선)
- **Unity 씬 작업**
  - Main Camera: Orthographic 전환, Position (0,0,-10), Background Type: Solid Color (#282828)
  - Player: `CharacterController` 제거 → `Rigidbody2D` (Gravity Scale=0, Continuous) + `CircleCollider2D`
  - DummyEnemy: `Rigidbody2D` (Body Type: Kinematic) + `CircleCollider2D` + SpriteRenderer 자식
  - `Directional Light` 삭제 (2D에서 불필요)

## 결정된 사항
- **물리 시스템**: Unity 2D 완전 전환 (Rigidbody2D + Physics2D)
- **맵 구성 방식**: Unity Tilemap 확정 (바닥 Tilemap + 벽 Tilemap + TilemapCollider2D)
- **Enemy 물리**: Kinematic Rigidbody2D (자체 이동 없음, 물리 충돌 영향 차단)
- **배경**: 진한 회색 단색 (#282828), Directional Light 제거
- **방향 처리**: PlayerController가 Z축 회전 → `transform.up` = 플레이어 전방으로 통일

## 미결 사항
- Tilemap 맵 미완성 — 바닥/벽 타일 미배치
- Inspector 재연결 미확인 (PlayerCamera Target, LockOnSystem PlayerCamera 필드)
- 전체 플레이 테스트 미실시 (이동·카메라·전투·패링 동작 확인 필요)

## 다음 작업 예정
- **Tilemap 맵 구성**
  1. Hierarchy → Grid 생성 → Tilemap (Floor) + Tilemap (Walls)
  2. Walls Tilemap에 TilemapCollider2D + CompositeCollider2D 추가
  3. Tile Palette로 바닥/벽 칠하기
- **Inspector 재연결**: PlayerCamera Target → Player, LockOnSystem → Main Camera
- **플레이 테스트**: 이동, 카메라 팔로우, 공격 판정, 패링, 락온 확인
